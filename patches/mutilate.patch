diff --git a/SConstruct b/SConstruct
index 57d0054..9399502 100644
--- a/SConstruct
+++ b/SConstruct
@@ -7,6 +7,7 @@ env = Environment(ENV = os.environ)
 env['HAVE_POSIX_BARRIER'] = True
 
 env.Append(CPPPATH = ['/usr/local/include', '/opt/local/include'])
+env.Append(LIBPATH= ['/home/mattia/Workspace/memcached-memwatched/lib/'])
 env.Append(LIBPATH = ['/opt/local/lib'])
 env.Append(CCFLAGS = '-std=c++11 -D_GNU_SOURCE')
 if sys.platform == 'darwin':
@@ -16,19 +17,19 @@ if sys.platform == 'darwin':
 conf = env.Configure(config_h = "config.h")
 conf.Define("__STDC_FORMAT_MACROS")
 if not conf.CheckCXX():
-    print "A compiler with C++11 support is required."
+    print("A compiler with C++11 support is required.")
     Exit(1)
-print "Checking for gengetopt...",
+print("Checking for gengetopt...", end=' ')
 if env.Execute("@which gengetopt &> /dev/null"):
-    print "not found (required)"
+    print("not found (required)")
     Exit(1)
-else: print "found"
+else: print("found")
 if not conf.CheckLibWithHeader("event", "event2/event.h", "C++"):
-    print "libevent required"
+    print("libevent required")
     Exit(1)
 conf.CheckDeclaration("EVENT_BASE_FLAG_PRECISE_TIMER", '#include <event2/event.h>', "C++")
 if not conf.CheckLibWithHeader("pthread", "pthread.h", "C++"):
-    print "pthread required"
+    print("pthread required")
     Exit(1)
 conf.CheckLib("rt", "clock_gettime", language="C++")
 conf.CheckLibWithHeader("zmq", "zmq.hpp", "C++")
@@ -37,8 +38,8 @@ if not conf.CheckFunc('pthread_barrier_init'):
 
 env = conf.Finish()
 
-env.Append(CFLAGS = ' -O3 -Wall -g')
-env.Append(CPPFLAGS = ' -O3 -Wall -g')
+env.Append(CFLAGS = '-O3 -Wall -g')
+env.Append(CPPFLAGS = '-O3 -Wall -g')
 
 env.Command(['cmdline.cc', 'cmdline.h'], 'cmdline.ggo', 'gengetopt < $SOURCE')
 
diff --git a/mutilate.cc b/mutilate.cc
index 426fd05..e9505ec 100644
--- a/mutilate.cc
+++ b/mutilate.cc
@@ -979,6 +979,13 @@ void do_mutilate(const vector<string>& servers, options_t& options,
     V("started at %f", get_time());
 
   start = get_time();
+  if (args.save_given) {
+    FILE *start_file;
+    if ((start_file = fopen("mutilate_start.log", "w")) == NULL)
+      DIE("--save: failed to open %s: %s", args.save_arg, strerror(errno));
+    fprintf(start_file, "%f\n",start);
+    fclose(start_file);
+  }
   for (Connection *conn: connections) {
     conn->start_time = start;
     conn->start(); // Kick the Connection into motion.
