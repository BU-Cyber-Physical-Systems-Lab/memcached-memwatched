diff --git a/SConstruct b/SConstruct
index 57d0054..71fec7d 100644
--- a/SConstruct
+++ b/SConstruct
@@ -2,33 +2,50 @@
 import os
 import sys
 
+target = ARGUMENTS.get('target', 'native')
+linkflags = ARGUMENTS.get('linkflags', '')
+
 env = Environment(ENV = os.environ)
 
+
 env['HAVE_POSIX_BARRIER'] = True
 
 env.Append(CPPPATH = ['/usr/local/include', '/opt/local/include'])
+env.Append(LIBPATH= [''])
+env.Append(LIBPATH= ['/home/mattia/Workspace/memcached-memwatched/lib/'])
+env.Append(LIBPATH= [''])
 env.Append(LIBPATH = ['/opt/local/lib'])
 env.Append(CCFLAGS = '-std=c++11 -D_GNU_SOURCE')
+
 if sys.platform == 'darwin':
     env['CC']  = 'clang'
     env['CXX'] = 'clang++'
+if linkflags !='':
+    env.Append(LINKFLAGS = linkflags.strip().split())
+
+if target !="native":
+        env['CC']=target + 'gcc',
+        env['CXX']=target + 'g++',
+        env['AR']=target + 'ar',
+        env['RANLIB']=target + 'ranlib',
+        env['LINK']=target + 'g++',
 
 conf = env.Configure(config_h = "config.h")
 conf.Define("__STDC_FORMAT_MACROS")
 if not conf.CheckCXX():
-    print "A compiler with C++11 support is required."
+    print("A compiler with C++11 support is required.")
     Exit(1)
-print "Checking for gengetopt...",
+print("Checking for gengetopt...", end=' ')
 if env.Execute("@which gengetopt &> /dev/null"):
-    print "not found (required)"
+    print("not found (required)")
     Exit(1)
-else: print "found"
+else: print("found")
 if not conf.CheckLibWithHeader("event", "event2/event.h", "C++"):
-    print "libevent required"
+    print("libevent required")
     Exit(1)
 conf.CheckDeclaration("EVENT_BASE_FLAG_PRECISE_TIMER", '#include <event2/event.h>', "C++")
 if not conf.CheckLibWithHeader("pthread", "pthread.h", "C++"):
-    print "pthread required"
+    print("pthread required")
     Exit(1)
 conf.CheckLib("rt", "clock_gettime", language="C++")
 conf.CheckLibWithHeader("zmq", "zmq.hpp", "C++")
@@ -37,8 +54,8 @@ if not conf.CheckFunc('pthread_barrier_init'):
 
 env = conf.Finish()
 
-env.Append(CFLAGS = ' -O3 -Wall -g')
-env.Append(CPPFLAGS = ' -O3 -Wall -g')
+env.Append(CFLAGS = '-O3 -Wall -g')
+env.Append(CPPFLAGS = '-O3 -Wall -g')
 
 env.Command(['cmdline.cc', 'cmdline.h'], 'cmdline.ggo', 'gengetopt < $SOURCE')
 
diff --git a/mutilate.cc b/mutilate.cc
index 426fd05..132fc8f 100644
--- a/mutilate.cc
+++ b/mutilate.cc
@@ -146,8 +146,8 @@ static bool s_send (zmq::socket_t &socket, const std::string &string) {
  * [IF WARMUP]  0:  Everyone: RUN for options.warmup seconds.
  * 1. Master <-> Agent: Synchronize
  * 2. Everyone: RUN for options.time seconds.
- * 3. Master -> Agent: Dummy message
- * 4. Agent -> Master: Send AgentStats [w/ RX/TX bytes, # gets/sets]
+ * 3. Master -> Agent: Dummy m(essage
+   .override {static=true;})* 4. Agent -> Master: Send AgentStats [w/ RX/TX bytes, # gets/sets]
  *
  * The master then aggregates AgentStats across all agents with its
  * own ConnectionStats to compute overall statistics.
@@ -979,6 +979,14 @@ void do_mutilate(const vector<string>& servers, options_t& options,
     V("started at %f", get_time());
 
   start = get_time();
+  if (args.save_given) {
+    FILE *start_file;
+    if ((start_file = fopen("mutilate_start.log", "w")) == NULL)
+      DIE("failed to open %s: %s", args.save_arg, strerror(errno));
+    fprintf(start_file, "%f\n",boot_time);
+    fclose(start_file);
+  }
+
   for (Connection *conn: connections) {
     conn->start_time = start;
     conn->start(); // Kick the Connection into motion.
@@ -1056,7 +1064,11 @@ void args_to_options(options_t* options) {
   //  if (args.no_record_scale_given)
   //    options->records = args.records_arg;
   //  else
-  options->records = args.records_arg / options->server_given;
+  if (options->server_given) {
+    options->records = args.records_arg / options->server_given;
+  } else {
+    options->records=0;
+  }
 
   options->binary = args.binary_given;
   options->sasl = args.username_given;
